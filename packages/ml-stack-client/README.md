# ml-stack-client

An HTTP client for a local model server: chat, completion, embeddings and
health, against anything that speaks the OpenAI-shaped API.

## Install

```
pip install ml-stack-client
```

Part of [ml-stack](https://github.com/adammikulis/ml-stack): train and run models across every machine on
your network.
