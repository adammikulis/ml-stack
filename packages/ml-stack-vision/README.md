# ml-stack-vision

Vision-language payloads, and a gate that checks the model on the other end
can actually see before you send it an image.

## Install

```
pip install ml-stack-vision
```

Part of [ml-stack](https://github.com/adammikulis/ml-stack): train and run models across every machine on
your network.
