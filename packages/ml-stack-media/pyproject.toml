[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ml-stack-media"
version = "0.1.0"
description = "WAV containers, image formats and model-asset downloads. Device tier: stdlib only."
requires-python = ">=3.11"
license = "Apache-2.0"
license-files = ["LICENSE"]
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/ml_stack"]
