# ml-stack-backend

One array API over MLX and PyTorch, so model math is written once and
runs on either. Import the ops, not the framework.

## Install

```
pip install ml-stack-backend
```

Part of [ml-stack](https://github.com/adammikulis/ml-stack): train and run models across every machine on
your network.
