# The ruff and pyright the ruff-* and pyright-errors budgets in budgets.json were counted
# with. A tool answering any other version leaves its metrics uncounted.
#
# To raise a pin: change the version here, install it, run `scripts/budgets --update`
# (`--allow-increase` if the new release finds more), and commit budgets.json alongside
# this file.
ruff==0.13.3
pyright==1.1.411
